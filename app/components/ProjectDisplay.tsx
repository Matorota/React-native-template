import React from "react";
import { View, Text } from "react-native";

interface ProjectDisplayProps {
  title: string;
  imageUrl: string;
  text: string;
  placeholder?: string;
  onPress: () => void;
  style?: any;
}
export default function ProjectDisplay({}: ProjectDisplayProps) {
  return;
  <view></view>;
}
